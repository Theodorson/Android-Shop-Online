<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shop_online">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".Session"
        android:allowBackup="true"
        android:icon="@mipmap/ic_book_shop_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_book_shop_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shop_Online">
        <activity android:name=".admin.AccountsActivity"></activity>
        <activity android:name=".admin.NewBookActivity" />
        <activity android:name=".admin.ManagementActivity" />
        <activity android:name=".order.OrdersActivity" />
        <activity android:name=".order.OrderActivity" />
        <activity android:name=".book.BookItemActivity" />
        <activity android:name=".login.RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>